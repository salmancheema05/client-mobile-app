export interface ThemeProps {
  primary: {
    dark: string;
    light: string;
    bg: string;
    shadowColor: string;
    circleBg: string;
    iconColor: string;
    // Add more color definitions as needed
  };
}
